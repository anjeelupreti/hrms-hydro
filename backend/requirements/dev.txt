-r base.txt

django-extensions>=3.2,<3.3
ipython>=8.27,<8.28

# --- Testing -------------------------------------------------------------
# pytest-django over Django's own runner because the tenant fixtures below
# need function-scoped schema creation, which fixtures express far better
# than TestCase inheritance.
pytest>=8.3,<9.0
# Pinned exactly: 4.13.0 crashes on the `db` fixture with
# "PytestDjangoTestCase has no attribute _pre_setup_ran_eagerly".
pytest-django==4.11.1
pytest-cov>=6.0,<7.0
factory-boy>=3.3,<4.0

# --- Linting -------------------------------------------------------------
ruff>=0.8,<1.0
